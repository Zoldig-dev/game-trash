{% extends 'layout/admin.html.twig' %}

{% block title %}Edit Society{% endblock %}

{% block body %}
    <div class="container">
        <div class="content d-flex flex-column">
            <h2 class="animate__animated animate__zoomInDown mb-2">Edit Society</h2>

            {{ include('admin/admin_society/_form.html.twig', {'button_label': 'Update'}) }}

            <a class='btn btn-outline-light' href='{{ path("society_index") }}'>back to list</a>
        </div>
    </div>

{% endblock %}
