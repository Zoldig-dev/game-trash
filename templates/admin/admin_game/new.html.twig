{% extends 'layout/admin.html.twig' %}

{% block title %}New Game{% endblock %}

{% block body %}
    <div class="container">
        <div class="content d-flex flex-column my-5">
            <h2 class="welcom animate__animated animate__jackInTheBox">Create new Game</h2>

            {{ include('admin/admin_game/_form.html.twig') }}

            <a class="btn btn-outline-light" href="{{ path('admin_game_index') }}">back to list</a>
        </div>
    </div>
{% endblock %}
