{% extends 'layout/user.html.twig' %}

{% block title %}Edit Topic{% endblock %}

{% block body %}
    <div class="container">

        {{ include('topic/_messageForm.html.twig') }}

        <a class="btn btn-outline-light" href="{{ path('topic', {'id': forum.id}) }}">back to list</a>
    </div>
{% endblock %}